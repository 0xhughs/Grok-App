# slices/

Completed BUILD.md pages are archived here when a slice ships.

Naming: `NN-slug.md`.

These files are history, not startup context. Open an archived slice only when current work depends on a past decision, a regression concerns that slice, or historical intent is needed.
